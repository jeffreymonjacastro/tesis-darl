\begin{tikzpicture}[
    font=\scriptsize,
    module/.style={
        draw,
        rounded corners=2pt,
        fill=gray!8,
        minimum height=15mm,
        text width=27mm,
        align=center
    },
    signal/.style={
        draw,
        rounded corners=2pt,
        fill=blue!8,
        minimum height=10mm,
        text width=24mm,
        align=center
    },
    agent/.style={
        draw,
        rounded corners=2pt,
        fill=orange!12,
        minimum height=15mm,
        text width=27mm,
        align=center
    },
    reward/.style={
        draw,
        rounded corners=2pt,
        fill=green!10,
        minimum height=15mm,
        text width=27mm,
        align=center
    },
    latent/.style={
        draw,
        dashed,
        rounded corners=2pt,
        fill=gray!3,
        minimum height=9mm,
        text width=82mm,
        align=center
    },
    arrow/.style={-{Latex[length=2.2mm]}, thick},
    signalflow/.style={-{Latex[length=2.2mm]}, thick, blue!70!black},
    actionflow/.style={-{Latex[length=2.2mm]}, thick, orange!80!black},
    feedback/.style={-{Latex[length=2.2mm]}, thick, dashed, green!45!black}
]

\node[latent] (state) at (5.2, 2.35)
{\textbf{Estado latente no observado $s_t$:}
$P_t(X)$, $P_t(Y\mid X)$, tipo y severidad};

\node[module] (data) at (0, 0.8)
{\textbf{1. Datos}\\TableShift\\pipeline base};

\node[module] (drift) at (3.45, 0.8)
{\textbf{2. Drift}\\Covariate shift\\inversión feature-label};

\node[module] (monitor) at (6.9, 0.8)
{\textbf{3. Monitoreo}\\PSI, KS, C2ST\\$\Delta$AUC, $\Delta$F1};

\node[signal] (obs) at (10.35, 0.8)
{\textbf{Observación}\\$o_t$};

\node[agent] (ppo) at (10.35, -1.65)
{\textbf{4. PPO}\\Actor-crítico\\$\pi_{\theta}(a_t\mid o_t)$};

\node[module] (action) at (6.9, -1.65)
{\textbf{5. Acción $a_t$}\\Defer / Features\\Model / Retrain};

\node[reward] (metric) at (3.45, -1.65)
{\textbf{6. Evaluación}\\AUC recuperado\\tiempo y RAM};

\draw[arrow] (data) -- (drift);
\draw[arrow] (drift) -- (monitor);
\draw[arrow] (state.south) -- (monitor.north);
\draw[signalflow] (monitor) -- node[above] {$o_t$} (obs);
\draw[signalflow] (obs) -- (ppo);
\draw[actionflow] (ppo) -- node[above] {$a_t$} (action);
\draw[actionflow] (action) -- (metric);
\draw[feedback] (metric.south) |- node[pos=0.25, below] {$r_t$ actualiza PPO} (ppo.south);
\draw[feedback] (action.north) |- node[pos=0.28, above] {pipeline actualizado} (monitor.south);

\end{tikzpicture}
